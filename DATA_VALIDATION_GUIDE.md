# 데이터 검증 및 품질 관리 가이드

## 📋 목차
1. [데이터 검증 개요](#데이터-검증-개요)
2. [검증 스크립트 실행 방법](#검증-스크립트-실행-방법)
3. [검증 항목 및 결과 해석](#검증-항목-및-결과-해석)
4. [이슈 해결 방법](#이슈-해결-방법)
5. [데이터 품질 리포트 API](#데이터-품질-리포트-api)
6. [발표 포인트](#발표-포인트)

---

## 데이터 검증 개요

### 검증 목적
- **데이터 일관성 보장**: 모든 데이터가 스키마와 규칙에 맞는지 확인
- **데이터 품질 관리**: 결측치, 이상치, 중복 데이터 자동 감지
- **시스템 안정성**: 잘못된 데이터로 인한 계산 오류 방지
- **신뢰성 확보**: UCI 점수와 등급 일치, 날짜 범위 검증 등

### 검증 범위
1. **Spatial Units**: 공간 단위 일관성
2. **Signals Human**: 민원 신호 데이터 검증
3. **Comfort Index**: UCI 점수 및 등급 검증
4. **Anomaly Signals**: 이상 탐지 결과 검증
5. **데이터 완전성**: 날짜 범위 커버리지 체크

---

## 검증 스크립트 실행 방법

### 1. 기본 실행

```bash
npm run validate-data
```

### 2. 직접 실행

```bash
node scripts/validate_data.js
```

### 3. 실행 결과 위치

**결과는 터미널(콘솔)에 실시간으로 출력됩니다.**

---

## 검증 항목 및 결과 해석

### 1. Spatial Units 일관성 체크

**검증 내용:**
- 총 공간 단위 개수 확인
- unit_id 유효성 검증

**정상 결과 예시:**
```
📊 Checking spatial units consistency...
  Found 37 spatial units
```

---

### 2. Signals Human 데이터 검증

**검증 내용:**
- ✅ **총 레코드 수**: 전체 데이터 건수
- ✅ **Invalid unit_id**: spatial_units에 없는 unit_id
- ✅ **Invalid date**: 날짜 형식 오류 (YYYY-MM-DD 형식 확인)
- ✅ **Invalid value**: 값 범위 오류 (음수, 비율 > 1 등)
- ✅ **Duplicates**: 중복 데이터 (unit_id + date + signal_type 조합)
- ✅ **Unique dates**: 고유한 날짜 개수

**정상 결과 예시:**
```
📊 Validating signals_human...
  Total records: 4071
  Invalid unit_id: 0
  Invalid date: 0
  Invalid value: 0
  Duplicates: 0
  Unique dates: 162
```

**이슈 발견 시:**
- `Invalid unit_id > 0`: spatial_units에 없는 unit_id 사용
- `Invalid date > 0`: 날짜 형식이 YYYY-MM-DD가 아님
- `Invalid value > 0`: 값이 범위를 벗어남 (예: 음수, 비율 > 1)
- `Duplicates > 0`: 동일한 조합의 데이터가 중복 저장됨

---

### 3. Comfort Index 검증

**검증 내용:**
- ✅ **Invalid uci_score**: UCI 점수가 0-100 범위를 벗어남
- ✅ **Invalid grade**: 등급이 A, B, C, D, E 중 하나가 아님
- ✅ **Grade mismatches**: 점수와 등급이 일치하지 않음

**등급 기준:**
- **A**: 0 ~ 19.99 (매우 양호)
- **B**: 20 ~ 39.99 (양호)
- **C**: 40 ~ 59.99 (보통)
- **D**: 60 ~ 79.99 (주의 필요)
- **E**: 80 ~ 100 (즉시 조치 필요)

**정상 결과 예시:**
```
📊 Validating comfort_index...
  Total records: 319
  Invalid uci_score: 0
  Invalid grade: 0
  Grade mismatches: 0
```

**이슈 발견 시:**
- `Grade mismatches > 0`: 점수와 등급이 불일치
  - 예: score=20인데 grade=A (올바른 등급: B)
  - 해결: `npm run fix-grade-mismatches` 실행

---

### 4. Anomaly Signals 검증

**검증 내용:**
- ✅ **Invalid scores**: anomaly_score가 0-1 범위를 벗어남

**정상 결과 예시:**
```
📊 Validating anomaly_signals...
  Total records: 62
  Invalid scores: 0
```

---

### 5. 데이터 완전성 체크

**검증 내용:**
- ✅ **Latest date**: 가장 최근 데이터 날짜
- ✅ **Date range checked**: 검증한 날짜 범위 (최근 30일)
- ✅ **Days with data**: 실제 데이터가 있는 날짜 수

**정상 결과 예시:**
```
📊 Checking data completeness...
  Latest date: 2025-12-01
  Date range checked: 2025-11-01 ~ 2025-12-01
  Days with data: 30 / 30
```

**이슈 발견 시:**
- `Days with data < 30`: 최근 30일 중 데이터가 부족
  - 예: `Days with data: 2 / 30 (6.7%)`
  - 해결: 더 많은 날짜의 데이터 임포트 필요

---

## 최종 리포트 해석

### 리포트 구조

```
============================================================
📋 Validation Report
============================================================
Total Issues: 2
  Critical: 0
  Warnings: 2

⚠️  Warnings:
  - comfort_index: Grade mismatch: score=20, expected=B, actual=A
  - signals_human: Low date coverage: 2/30 days (6.7%)

⚠️  Found 2 issues. Please review and fix.
```

### 이슈 등급

#### 🔴 Critical (즉시 수정 필요)
- 잘못된 날짜 형식
- 중복 데이터
- UCI 점수 범위 초과 (0-100 벗어남)
- 등급 형식 오류 (A-E가 아님)

#### ⚠️ Warnings (검토 권장)
- 등급과 점수 불일치
- 값 범위 경고 (음수, 비율 초과 등)
- 데이터 완전성 부족 (날짜 커버리지 낮음)
- unit_id 매칭 실패 (구 단위 코드는 정상)

---

## 이슈 해결 방법

### 1. Grade Mismatch 수정

**문제:**
```
Grade mismatch: score=20, expected=B, actual=A
```

**원인:**
- 등급 경계값 처리 불일치
- 점수 20은 B 등급이어야 하는데 A로 저장됨

**해결 방법:**

```bash
# Grade mismatch 자동 수정 스크립트 실행
npm run fix-grade-mismatches
```

**수동 수정:**
```javascript
// MongoDB에서 직접 수정
db.comfort_index.updateMany(
  { uci_score: 20, uci_grade: "A" },
  { $set: { uci_grade: "B" } }
);
```

---

### 2. 데이터 완전성 개선

**문제:**
```
Low date coverage: 2/30 days (6.7%)
```

**원인:**
- 데이터 임포트가 최근 날짜에만 집중됨
- 여러 날짜에 대한 데이터가 부족

**해결 방법:**

#### 방법 1: 여러 날짜에 대해 UCI 계산

```bash
# 최근 365일간 Human Signal 데이터가 있는 모든 날짜에 대해 UCI 계산 (기본값)
npm run compute-uci-multiple

# 또는 특정 기간 지정
npm run compute-uci-multiple 365
# (최근 365일)

# 모든 데이터에 대해 계산 (매우 긴 기간)
node scripts/compute_uci_multiple_dates.js 9999
```

**참고:**
- 데이터가 월별로만 있는 경우 (예: 2025-03-01, 2025-04-01), 최근 30일 내에는 데이터가 없을 수 있습니다.
- 이 경우 더 긴 기간(365일 이상)을 지정하거나, 모든 데이터에 대해 계산하세요.

#### 방법 2: 더 많은 날짜의 데이터 임포트

```bash
# Human Signal 데이터 임포트
npm run etl

# Population Signal 데이터 임포트
npm run etl-population

# 구 단위 민원 데이터 임포트
npm run etl-district
```

#### 방법 3: 데이터 보강 (결측치 채우기)

```bash
# API를 통한 데이터 보강
POST /api/v1/analytics/augment
{
  "unit_id": "11110",
  "start_date": "2025-11-01",
  "end_date": "2025-12-01",
  "signal_type": "human"
}
```

---

### 3. Invalid unit_id 해결

**문제:**
```
Invalid unit_id: 5
```

**원인:**
- spatial_units에 없는 unit_id를 사용하는 데이터 존재
- 구 단위 코드(5자리)는 정상이지만, 행정동 코드(8자리)가 spatial_units에 없을 수 있음

**해결 방법:**

1. **spatial_units에 누락된 unit 추가:**
```bash
# Spatial Unit 생성 API 사용
POST /api/v1/units
{
  "unit_id": "11110515",
  "name": "청운효자동",
  "geom": { ... }
}
```

2. **잘못된 unit_id 데이터 삭제:**
```javascript
// MongoDB에서 확인 후 삭제
db.signals_human.find({ unit_id: "INVALID_ID" });
db.signals_human.deleteMany({ unit_id: "INVALID_ID" });
```

---

### 4. 중복 데이터 제거

**문제:**
```
Duplicates: 10
```

**해결 방법:**

```javascript
// MongoDB에서 중복 데이터 찾기
db.signals_human.aggregate([
  {
    $group: {
      _id: { unit_id: "$unit_id", date: "$date", signal_type: "$signal_type" },
      count: { $sum: 1 },
      ids: { $push: "$_id" }
    }
  },
  { $match: { count: { $gt: 1 } } }
]);

// 중복 데이터 중 가장 최근 것만 남기고 삭제
// (수동으로 처리 필요)
```

---

## 데이터 품질 리포트 API

### API 엔드포인트

```
GET /api/v1/analytics/data-quality
```

### 파라미터

- `unit_id` (선택): 특정 지역만 조회
- `start_date` (필수): 시작 날짜 (YYYY-MM-DD)
- `end_date` (필수): 종료 날짜 (YYYY-MM-DD)

### 응답 예시

```json
{
  "success": true,
  "report_date": "2026-01-08",
  "unit_id": "11110",
  "date_range": {
    "start": "2025-11-01",
    "end": "2025-12-01"
  },
  "completeness_score": 87.33,
  "missing_data_points": 5,
  "outliers_detected": 12,
  "quality_score": 87.33,
  "details": {
    "human_signals": {
      "completeness": 83.33,
      "missing_days": 5
    },
    "population_signals": {
      "completeness": 93.33,
      "missing_days": 2
    }
  }
}
```

### 사용 예시

```bash
# 전체 데이터 품질 리포트
curl "http://localhost:8000/api/v1/analytics/data-quality?start_date=2025-11-01&end_date=2025-12-01"

# 특정 지역 데이터 품질 리포트
curl "http://localhost:8000/api/v1/analytics/data-quality?unit_id=11110&start_date=2025-11-01&end_date=2025-12-01"
```

---

## 발표 포인트

### 1. 자동화된 데이터 검증 시스템

**핵심 메시지:**
- "데이터 품질을 자동으로 검증하고 이슈를 감지하는 시스템을 구축했습니다."

**기술적 특징:**
- ✅ 5가지 검증 카테고리 (Spatial Units, Signals, UCI, Anomaly, Completeness)
- ✅ Critical/Warning 등급 분류
- ✅ 실시간 검증 리포트 생성
- ✅ API를 통한 데이터 품질 모니터링

**발표 시나리오:**
```
"데이터 검증은 시스템의 신뢰성을 보장하는 핵심 기능입니다.
우리는 5가지 카테고리에서 자동으로 데이터를 검증하고,
Critical과 Warning으로 분류하여 즉시 대응할 수 있도록 했습니다.
예를 들어, UCI 점수와 등급이 일치하지 않는 경우를 자동으로 감지하고,
한 번의 명령으로 모든 불일치를 수정할 수 있습니다."
```

---

### 2. 데이터 품질 관리 프로세스

**핵심 메시지:**
- "데이터 품질을 지속적으로 모니터링하고 개선하는 프로세스를 구축했습니다."

**프로세스:**
1. **정기 검증**: `npm run validate-data` 주기적 실행
2. **이슈 감지**: Critical/Warning 자동 분류
3. **자동 수정**: Grade mismatch 등 자동 수정 스크립트
4. **데이터 보강**: API를 통한 결측치 자동 채우기
5. **품질 리포트**: API를 통한 실시간 모니터링

**발표 시나리오:**
```
"데이터 품질 관리는 단순히 검증하는 것이 아니라,
발견된 이슈를 자동으로 수정하고, 부족한 데이터를 보강하는
전체 프로세스를 구축했습니다.
예를 들어, 최근 30일 중 데이터가 6.7%만 있는 경우를 감지하면,
자동으로 데이터 보강 API를 통해 결측치를 채울 수 있습니다."
```

---

### 3. 데이터 일관성 보장

**핵심 메시지:**
- "데이터 일관성을 보장하여 계산 결과의 신뢰성을 확보했습니다."

**검증 항목:**
- ✅ unit_id 매칭 검증 (spatial_units와 일치)
- ✅ 날짜 형식 검증 (YYYY-MM-DD)
- ✅ 값 범위 검증 (UCI 0-100, 비율 0-1 등)
- ✅ 중복 데이터 감지
- ✅ UCI 점수-등급 일치 검증

**발표 시나리오:**
```
"데이터 일관성은 정확한 UCI 계산의 기반입니다.
우리는 unit_id 매칭, 날짜 형식, 값 범위, 중복 데이터 등
다양한 측면에서 데이터를 검증하여,
계산 결과의 신뢰성을 보장합니다.
예를 들어, UCI 점수가 20인데 등급이 A로 저장된 경우를
자동으로 감지하고 B로 수정합니다."
```

---

### 4. 실시간 데이터 품질 모니터링

**핵심 메시지:**
- "API를 통해 실시간으로 데이터 품질을 모니터링할 수 있습니다."

**기능:**
- ✅ 완전성 점수 (0-100)
- ✅ 결측치 개수
- ✅ 이상치 감지
- ✅ 신호별 상세 리포트

**발표 시나리오:**
```
"데이터 품질 리포트는 프론트엔드 대시보드에서도 확인할 수 있도록
API로 제공됩니다. 완전성 점수, 결측치, 이상치 등을 실시간으로
모니터링하여, 데이터 품질 문제를 조기에 발견하고 대응할 수 있습니다."
```

---

## 체크리스트

### 정기 검증 (주 1회 권장)

- [ ] `npm run validate-data` 실행
- [ ] Critical 이슈 확인 및 수정
- [ ] Warning 이슈 검토
- [ ] 데이터 완전성 확인

### 데이터 임포트 후

- [ ] `npm run validate-data` 실행
- [ ] Invalid unit_id 확인
- [ ] 중복 데이터 확인
- [ ] 날짜 범위 확인

### UCI 계산 후

- [ ] `npm run validate-data` 실행
- [ ] Grade mismatch 확인
- [ ] `npm run fix-grade-mismatches` 실행 (필요 시)

---

## 관련 스크립트

| 스크립트 | 설명 | 실행 방법 |
|---------|------|----------|
| `validate_data.js` | 데이터 검증 | `npm run validate-data` |
| `fix_grade_mismatches.js` | Grade mismatch 수정 | `npm run fix-grade-mismatches` |
| `compute_uci_multiple_dates.js` | 여러 날짜 UCI 계산 | `npm run compute-uci-multiple` |

---

## 참고 자료

- [API 문서](http://localhost:8000/docs)
- [README.md](./README.md)
- [API_ENDPOINTS_GUIDE.md](./API_ENDPOINTS_GUIDE.md)

---

**마지막 업데이트**: 2026-01-12

